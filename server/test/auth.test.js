import { expect } from 'chai';

describe('auth routes', () => {
	it.skip('POST /auth/login should reject invalid credentials', async () => {
		// Pending: requires DB or mocking
		expect(true).to.be.true;
	});
});
